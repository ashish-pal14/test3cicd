name: activity types

on:
  push: 
    branches: 
      - main
    branches-ignore:
      - ashish
      
  pull_request:
    types: [closed, reopened]
  
      
      

jobs:
  jon1:
   runs-on: ubuntu-latest
   steps:
      - name: check test
        run: echo "${{github.actor}}"
   